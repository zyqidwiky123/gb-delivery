{
  "rules": {
    "drivers": {
      ".read": "auth != null",
      "$uid": {
        ".read": "auth != null",
        ".write": "auth.uid === $uid",
        "location": {
          ".read": "auth != null",
          ".write": "auth.uid === $uid"
        }
      }
    }
  }
}
